package pe.dcs.app.util.enums.space_reservation;

/** Estado de una reserva de espacio — se crea CONFIRMED directamente (sin flujo de aprobación en v1). */
public enum ReservationStatus {
    CONFIRMED,
    CANCELLED
}
