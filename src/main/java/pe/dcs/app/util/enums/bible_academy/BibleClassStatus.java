package pe.dcs.app.util.enums.bible_academy;

/** Estado de un dictado (BibleClass) — la instancia concreta que una sede abre de un curso. */
public enum BibleClassStatus {
    PLANNED,
    IN_PROGRESS,
    COMPLETED,
    CANCELLED
}
